"""Servidor MCP (FastMCP) para o Eizō.

Pacote interno — use `eizo.mcp.server.create_server`/`serve_mcp` via o
comando `eizo mcp`. A existência deste `__init__.py` garante que o pacote
seja incluído no wheel pelo `find_packages` (ver pyproject.toml).
"""
