"""Parsers por linguagem (Tree-sitter) para o Eizō.

Pacote interno — importe via `eizo.indexer` ou diretamente os módulos
(`eizo.parser.python`, `eizo.parser.typescript`, ...). A existência deste
`__init__.py` garante que o pacote seja incluído no wheel pelo
`find_packages` (ver pyproject.toml).
"""
